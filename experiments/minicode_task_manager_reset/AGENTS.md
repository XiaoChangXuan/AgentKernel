# MiniCode Task Manager Challenge Instructions

You are working inside a deterministic Python coding challenge.

Rules:

- Do not modify `tests/`.
- Modify source files with `apply_patch`.
- Start by running `python -m pytest -q` to observe the current failures.
- Use search/read tools to inspect the implementation before patching.
- Fix one category of behavior at a time.
- After each category of fix, run the related tests.
- If tests fail, continue from the real pytest output.
- Run the full `python -m pytest -q` before finishing.
- Finish only after the complete test suite passes.

The tests are the task specification.
